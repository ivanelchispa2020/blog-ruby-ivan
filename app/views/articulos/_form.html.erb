<%= form_for(articulo) do |f| %>
  <% if articulo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(articulo.errors.count, "error") %> prohibited this articulo from being saved:</h2>

      <ul>
      <% articulo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %>
    <%= f.text_field :titulo %>
  </div>

  <div class="field">
    <%= f.label :parrafo %>
    <%= f.text_area :parrafo %>
  </div>

  <div class="field">
    <%= f.label :parrafo_principal %>
    <%= f.text_area :parrafo_principal %>
  </div>

  <div class="field">
    <%= f.label :autor %>
    <%= f.text_field :autor %>
  </div>

  <div class="field">
    <%= f.label :fecha_subida %>
    <%= f.datetime_select :fecha_subida %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
